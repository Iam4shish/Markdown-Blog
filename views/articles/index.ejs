<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Explore our latest blog articles on various topics.">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <title>Blog</title>
    <style>
        body {
            position: relative;
            overflow: auto; /* Allow scrolling */
            background-color: #d4edda; /* Calm green background */
            height: 100vh; /* Full viewport height */
            color: #1b1b1b; /* Dark text for good contrast */
            font-family: Arial, sans-serif; /* Clean font for better readability */
        }
        .container {
            padding: 20px; 
            min-height: 100vh; 
            background-color: #ffffff; 
            border: 2px solid #155724; 
            border-radius: 10px; /* Rounded corners */
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2); /* Subtle shadow for depth */
            margin-top: 20px; /* Space from the top */
        }
        h1 {
            text-align: center; /* Center the heading */
            font-weight: bold; /* Bold text */
            color: #155724; /* Dark green for a professional look */
            margin-bottom: 30px; /* Add space below the heading */
            font-size: 2.5rem; /* Larger font size for prominence */
        }
        .card {
            border: 1px solid #28a745; /* Green border for blog cards */
            background-color: #f8f9fa; /* Light gray background for contrast with container */
            color: #155724; /* Dark green text for titles */
            transition: transform 0.3s ease; /* Smooth transform effect */
        }
        .card:hover {
            transform: scale(1.03); /* Slightly enlarge the card on hover */
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.3); /* Enhanced shadow on hover */
        }
        .card-title {
            color: #155724; 
            font-weight: bold;
        }
        .btn {
            border-radius: 10px;
            font-weight: bold; 
            padding: 10px 20px; 
        }
        .btn-success {
            background-color: #1d611d; 
            color: white;
            border: none;
            transition: background-color 0.3s; /* Smooth color change */
        }
        .btn-success:hover {
            background-color: #4cae4c; /* Darker green on hover */
        }
        .btn-primary {
            background-color: #046ee0; /* Bright blue for primary actions */
            color: white; /* White text for better contrast */
            border: none;
            transition: background-color 0.3s; /* Smooth color change */
        }
        .btn-primary:hover {
            background-color: #0056b3; /* Darker blue on hover */
        }
        .btn-info {
            background-color: #179eb3; /* Teal for edit buttons */
            color: white; /* White text for better contrast */
            border: none;
            transition: background-color 0.3s; /* Smooth color change */
        }
        .btn-info:hover {
            background-color: #0b545f; /* Darker teal on hover */
        }
        .btn-danger {
            background-color: #dc3545; /* Bright red for delete buttons */
            color: white; /* White text for better contrast */
            border: none;
            transition: background-color 0.3s; /* Smooth color change */
        }
        .btn-danger:hover {
            background-color: #761821; 
        }
        .badge-primary {
            background-color: #155724; 
        }
        .badge-secondary {
            background-color: #6c757d; /* Neutral gray for tag badges */
        }
        .footer {
            text-align: center; /* Centered footer */
            padding: 20px; /* Space around footer */
            background-color: #1e8636; /* Bright green for footer */
            color: white; /* White text */
            margin-top: 20px; /* Space from the main content */
            border-radius: 0 0 10px 2px; /* Rounded bottom corners */
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>THE BLOG</h1>
        <a href="/articles/new" class="btn btn-success mb-3">New Article</a>

        <% if (articles.length === 0) { %>
            <div class="alert alert-warning">No articles available.</div>
        <% } else { %>
            <% articles.forEach(article => { %>
                <div class="card mt-4">
                    <div class="card-body">
                        <h4 class="card-title"><%= article.title %></h4>
                        <div class="card-subtitle text-muted mb-2">
                            <%= article.createdAt.toLocaleDateString() %>
                        </div>
                        <div class="mb-2">
                            <% if (article.category) { %>
                                <span class="badge badge-primary"><%= article.category %></span>
                            <% } %>
                            <% (article.tags || []).forEach(tag => { %>
                                <span class="badge badge-secondary"><%= tag %></span>
                            <% }) %>
                        </div>
                        <div class="card-text mb-2"><%= article.description %></div>
                        <a href="/articles/<%= article.slug %>" class="btn btn-primary">Read More</a>
                        <a href="/articles/edit/<%= article.id %>" class="btn btn-info">Edit</a>
                        <form action="/articles/<%= article.id %>?_method=DELETE" method="POST" class="d-inline">
                            <button type="submit" class="btn btn-danger" aria-label="Delete article: <%= article.title %>" onclick="return confirm('Are you sure you want to delete this article?')">Delete</button>
                        </form>
                    </div>
                </div>
            <% }) %>
        <% } %>
    </div>

    <div class="footer">
        <p>&copy; 2024 Gouri Biswas. All rights reserved.</p>
    </div>

    <script src="https://code.jquery.com/jquery-3.2.1.slim.min.js" integrity="sha384-KyZXEAg3QhqLMpG8r+Knujsl5+6R0C4mHp3MO8f4UshnGAP1KjZ4I5i5zK6gGOTc" crossorigin="anonymous"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.11.0/umd/popper.min.js" integrity="sha384-4R6z2WIpZZp7HQ2g8gTzPp4IWg8Yp/6u5xCk1N2eCkcL9V2X0Z6KX94vM7F1cNho" crossorigin="anonymous"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js" integrity="sha384-JZR6Spejh4Q02j3cF3B48rM13H00M5T9eD7F8W41ORrD6+PlcRygfV2zRj2wJ5Ic" crossorigin="anonymous"></script>
</body>
</html>
